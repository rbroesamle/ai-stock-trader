import numpy as np
from sklearn.linear_model import LinearRegression


def get_x_y_array_from_companies(all_companies):
    # all_x_y_tuples = []
    for company in all_companies:
        for score in company.average_score:
            break
        # all_tuples.append
